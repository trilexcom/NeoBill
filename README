PLEASE NOTE:
The development of NeoBill. The current commit 
in the trunc is a alpha development release of  v0.9. It uses a new 
configuration file format and a new database scheme which both might change 
until the release of v0.9 without update/upgrade support.